pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                terraform {    
                sh "terraform --version"               
            }
        }
    }
}
